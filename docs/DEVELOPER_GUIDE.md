# Руководство разработчика

Как устроен проект, как его гонять и почему он устроен именно так. Быстрый
старт — в [QUICKSTART.md](QUICKSTART.md), слои и потоки данных — в
[ARCHITECTURE.md](ARCHITECTURE.md), эксплуатация и миграции — в
[OPERATIONS_AND_MIGRATIONS.md](OPERATIONS_AND_MIGRATIONS.md).

- [Что за проект](#что-за-проект)
- [Текущий статус проекта](#текущий-статус-проекта)
- [Архитектура](#архитектура)
- [Запуск тестов](#запуск-тестов)
  - [Структура тестов](#структура-тестов)
  - [Живой и браузерный прогоны](#живой-и-браузерный-прогоны)
  - [Почта в разработке](#почта-в-разработке)
  - [Изоляция набора от машины](#изоляция-набора-от-машины)
  - [CI: набор гоняется дважды](#ci-набор-гоняется-дважды)
  - [Чего набор не держит](#чего-набор-не-держит)
  - [Флаги прогона](#флаги-прогона)
- [Жизненный цикл гостевой ссылки](#жизненный-цикл-гостевой-ссылки)
- [Удаление гостевой ссылки](#удаление-гостевой-ссылки)
- [Конфигурация](#конфигурация)
- [CLI-команды](#cli-команды)
- [Ключевые файлы](#ключевые-файлы)
- [Документация API](#документация-api)
- [Как добавить новый use case](#как-добавить-новый-use-case)
- [Принятые решения и границы](#принятые-решения-и-границы)

## Что за проект

Полнофункциональный сервис сокращения ссылок на Python/Flask, построенный по принципам Clean Architecture. Поддерживает:

- **Гостевое создание ссылок** — кто угодно может сократить URL без аккаунта (ссылки истекают через 7 дней по умолчанию)
- **Аккаунты пользователей** — зарегистрированные пользователи получают постоянные ссылки, личную статистику и панель управления
- **RBAC** — контроль доступа на основе ролей с четырьмя ролями: guest, user, analyst, admin
- **Асинхронную статистику** — подсчёт кликов через Celery
- **Кэширование** — двухуровневый кэш (L1: редиректы, L2: объекты ссылок) с инвалидацией при удалении
- **Rate limiting** — защита от brute-force на auth-эндпоинтах
- **Аудит** — структурированное логирование всех значимых событий
- **Health check кэширование** — снимок зависимостей кэшируется 2 сек

## Текущий статус проекта

**Альфа / Разработка**. Основной функционал работает:
- Создание ссылок (поштучное и пакетное)
- Гостевой и авторизованный режимы с TTL
- Регистрация пользователей и JWT-аутентификация
- Удаление ссылок с инвалидацией кэша и логированием ошибок
- Пагинация для списка ссылок пользователя
- Админ-панель с управлением пользователями/ролями
- Rate limiting на auth-эндпоинтах
- Мониторинг здоровья сервиса с кэшированием
- CLI-команды для обслуживания

**Известные ограничения:**
- Нет HTTPS в режиме разработки
- Нет link preview / OG-тегов
- Нет версионирования API выше v1
- Нагрузочный профиль не снимался: пулы соединений и таймауты выставлены по
  рассуждению, а не по замеру
- CI гоняет только тесты. Линтеры и типы (`flake8`, `black`, `isort`,
  `mypy`, `pylint`, `bandit`) стоят в dev-группе, но запускаются вручную
- `tests/live/smoke_test.py` и `tests/live/browser_test.py` pytest не собирает
  (`python_files = "test_*.py"` не матчит имена), поэтому запускать их надо
  руками — см. [Живой и браузерный прогоны](#живой-и-браузерный-прогоны)

## Архитектура

```
src/link_shortener/
├── domain/              # Сущности, объекты-значения, интерфейсы репозиториев
├── application/         # Use case'ы, DTO, сервисы приложения (фасады), порты
├── infrastructure/      # Конкретные реализации: БД, кэш, авторизация, DI, CLI
└── web/                 # Flask-контроллеры, middleware, шаблоны, статические файлы
```

**Ключевые паттерны:**
- **Repository** — абстрактные интерфейсы хранения в `domain/`, реализации SQLAlchemy в `infrastructure/`
- **Unit of Work** — управление транзакциями через контекстные менеджеры
- **Dependency Injection** — компонентный контейнер в `infrastructure/di/` с lazy-инициализацией
- **Facade** — `LinkService` и `AdminService` упрощают доступ веб-слоя к use case'ам
- **Decorator** — `@login_required` и `@require_permission` для аутентификации/авторизации
- **Failover** — логгер и кэш деградируют без ошибок. При `LOGGER_TYPE` и
  `AUDIT_TYPE` со значением `auto` (умолчание) или `structlog` цепочка —
  ровно две реализации, structlog → standard; при `standard` порядок
  обратный. Null позади них не ставится: вызов, который отвергли обе,
  отвергается явно, а не глохнет. При `null`, а также при
  `LOGGING_ENABLED=false` или `AUDIT_ENABLED=false`, реализация одна и
  failover не создаётся вовсе. Наверх работа возвращается не чаще раза в
  пять минут: кулдаун тратится проверкой, которой было что делать, и
  переживает само повышение — иначе сервис, который здоров по опросу и
  падает на вызове, забирал бы работу на каждой проверке. Вниз работа
  уходит двумя путями: вызов, бросивший исключение, и опрос здоровья на
  фоновой проверке. Второй нужен потому, что отказ, ради которого этот
  механизм существует, исключения не бросает: `StandardLogger` без
  обработчиков отвечает `is_healthy() == False`, а вызовы принимает молча

## Запуск тестов

Менеджер пакетов: **uv**

Проект использует три уровня тестирования:

```bash
# Уровень 1: Unit-тесты (моки, изолированно)
uv run pytest tests/unit/ -v

# Уровень 2: Интеграционные тесты (реальная in-memory SQLite)
# --ignore нужен, иначе соберётся и уровень 2b, требующий Docker
uv run pytest tests/integration/ --ignore=tests/integration/docker/ -v

# Уровень 2b: Интеграционные тесты (реальный PostgreSQL + Redis)
# Docker-сервисы поднимаются автоматически
uv run pytest tests/integration/docker/ -v

# Уровень 3: E2E тесты (полные пользовательские сценарии)
uv run pytest tests/e2e/ -v

# Все тесты вместе
uv run pytest tests/ -v

# С покрытием
uv run pytest tests/ --cov=src/link_shortener --cov-report=term-missing
```

Тесты: 2300 (unit + integration + e2e), покрытие: 92.45% при пороге 88%

### Структура тестов

```
tests/
├── unit/                          # Моки, изолированно
│   ├── domain/                    # Сущности, value objects, политики
│   ├── application/               # Use cases, services, порты
│   ├── infrastructure/            # Config, cache, task queue
│   │   ├── test_auth/             # JWT: обязательные claim'ы, контракт authenticate()
│   │   ├── test_failover/         # Падение на запасной и возврат наверх
│   │   └── test_logging/          # Форматтеры, адаптеры логгеров и аудита
│   └── web/                       # Controllers, middleware, security, schemas
│
├── integration/                   # Реальная in-memory SQLite
│   ├── application/               # Кэш против БД, удаление, кастомные коды
│   ├── infrastructure/database/   # Repository CRUD, UoW, миграции
│   ├── web/controllers/           # API, Auth, Admin
│   ├── web/middleware/            # Authentication, CSRF, лимиты по маршрутам
│   ├── web/test_templates/        # Шаблоны против маршрутов
│   ├── cli/                       # CLI команды
│   └── docker/                    # Реальный PostgreSQL + Redis (Docker)
│
├── e2e/                           # Полные пользовательские сценарии
└── live/                          # Smoke test всех эндпоинтов
```

> **Часы внедряются, а не пережидаются.** `FailoverService` возвращает
> работу наверх не раньше, чем истечёт кулдаун в пять минут. Единственный
> способ увидеть это, не внедряя часы, — проспать пять минут, поэтому
> конструктор принимает `clock`, а тест двигает время рукой.
>
> `FakeClock` стартует с реалистичной эпохи намеренно. Внутри службы
> «ни разу не пробовали» записано как `0.0`, то есть 1970 год, и работает
> это лишь потому, что настоящие часы показывают величину много больше
> любого кулдауна. Часы, стартующие около нуля, увели бы тест на ветку,
> которой в проде не бывает.
>
> Проверяется обе стороны. Только падение на запасной — это служба, которая
> заканчивает жизнь на null-реализации и об этом никто не узнает.

### Живой и браузерный прогоны

> **Живой прогон.** `uv run python tests/live/smoke_test.py` — 114 проверок
> по HTTP против настоящего приложения, не через pytest. Код возврата
> ненулевой, если хоть одна упала или если проверок оказалось не 114:
> «всё прошло» — утверждение о тех проверках, которые запустились, и
> ничего не говорит о переставших запускаться.
>
> Покрытие маршрутов при этом не заявляется, а считается: прогон
> запоминает, какое правило ответило на каждый запрос, и краснеет, если
> какое-то не было задето ни разу. Не задето ровно одно — `/static`,
> и оно названо в самом файле. Админский API пройден с обеих сторон:
> админа скрипт заводит сам, записывая роль в базу — так же, как это
> делает оператор, потому что первого администратора назначить некому.
> Через него же пройден `stats:view_full`.
>
> **Браузерный прогон.** `uv run python tests/live/browser_test.py` — девять
> проверок настоящим браузером (Playwright, chromium) против сервера на
> реальном порту. Он покрывает то, чего не видит ни набор, ни живой прогон:
> `web/static/js/pages/*.js` исполняется браузером, а не упоминается в
> разметке. Проверяется, что формы показывают человеку фразу, а не
> машинный код: замерено — разворот `data.message || data.error` во всех
> восьми файлах роняет три проверки из девяти (`VALIDATION_ERROR`,
> `VALIDATION_ERROR`, `INVALID_CREDENTIALS`), тогда как весь набор и живой
> прогон остаются зелёными.
>
> В CI он не включён — это решение владельца 2026-08-10: браузер в CI —
> новый вид прогона со своей установкой и своим источником нестабильных
> падений. Запускается руками, как и живой прогон:
> `uv sync --group browser` и один раз `uv run playwright install chromium`.
>
> Playwright объявлен отдельной группой `browser`, а не в `dev`.
> `requirements.txt` экспортируется из lock-файла без фильтрации групп, и
> Dockerfile ставит именно его, поэтому всё из `dev` едет в рантайм-образ.
> Замерено: с playwright в `dev` образ вырос с 540 МБ до 731 МБ — ради
> браузера, который сервис никогда не запускает. Экспорт идёт с
> `--no-group browser`, и CI сверяет файл этой же командой.
>
> База — файл во временном каталоге, не `:memory:`: сервер отвечает в
> рабочих потоках, а in-memory SQLite между их соединениями не разделяется.
> Адрес прогона передаётся в `CORS_ORIGINS` и `BASE_URL`: браузер шлёт
> настоящий `Origin` со случайным портом, и без этого CSRF-слой отвергает
> каждую отправку формы, что читается как восемь сломанных скриптов.

> Клиентов пять, и это не удобство. Тестовый клиент Flask держит cookie, так
> что после входа **любой** его запрос — запрос с cookie, а небезопасный
> запрос с cookie обязан нести CSRF-токен и без него получает 403 ещё до
> входа в логику. Один общий клиент поэтому и превращал каждый последующий
> POST и DELETE в отказ, а каждую «анонимную» проверку — в проверку
> вошедшего пользователя: 32 из 56.
>
> - `guest` не входит никогда и потому действительно аноним;
> - `api` носит `Authorization: Bearer` и не держит cookie — CSRF к нему не
>   применяется по построению;
> - `session_client` входит и хранит cookie, то есть ведёт себя как браузер;
>   каждый его небезопасный запрос идёт через `csrf()`;
> - `admin` заводится записью роли в базу — первого администратора назначить
>   некому — и через него проходит админский API с обеих сторон;
> - `stranger` — вторая учётная запись: вошедшая и не имеющая прав на чужое.
>   Без неё в файле есть только владелец и аноним, а «вошёл, но не твоё» —
>   третий ответ, ради которого пообъектные проверки и написаны: без этого
>   клиента их все можно удалить из приложения, и прогон останется зелёным.
>
> Регистрации идут каждая со своего адреса: лимит на этом эндпоинте — три в
> час на адрес, и с одного адреса четвёртый сценарий мерил бы лимит вместо
> того, что назван в его имени.

> **Docker-тесты не пропускаются молча.** Недоступный демон Docker — это
> законный пропуск: машина не может их выполнить. Всё остальное — отказ.
> Различие появилось не сразу: однажды тестовый стек не поднялся из-за
> занятого порта, все ветки отчитались `skipped`, и прогон вернулся зелёным
> — `492 passed, 16 skipped` вместо прежних `508 passed`. Заметил это
> только подсчёт.

> **Миграции проверяются отдельно.** `integration/infrastructure/database/
> test_migrations.py` прогоняет цепочку ревизий на настоящем файле БД.
> Остальные тесты строят схему через `create_all` из моделей и ревизий не
> исполняют — из-за чего сломанная миграция долго оставалась незамеченной.

### Почта в разработке

Разработческая надстройка поднимает Mailpit — ловец писем, который
принимает SMTP на 1025 и ничего никуда не отправляет. Всё пойманное видно
на `http://127.0.0.1:8025`.

```bash
docker compose --env-file .env.docker up -d mailpit
```

`DevelopmentConfig` уже нацелен на него: `MAIL_HOST=localhost`,
`MAIL_PORT=1025`, `MAIL_USE_TLS=false`. Остаётся включить сам канал —
`MAIL_ENABLED=true` в `.env`.

Почему ловец, а не «в dev пишем письма в файл»: через него проходит ровно
тот код, который работает в бою — `smtplib`, диалог SMTP, сборка
заголовков. Ветка «а в разработке по-другому» этот путь не проверяет и
ломается незаметно.

TLS ловец не предлагает, и это единственное место, где отправка идёт в
открытую: трафик не покидает машину, а пароля нет вовсе. Если задать
`MAIL_USERNAME`, конфигурация откажется стартовать — вместо того чтобы
отправить пароль открытым текстом.

Набор тестов не отправляет писем ни при какой настройке машины:
`TestingConfig` объявляет `MAIL_ENABLED = False` плоским атрибутом, а он
перекрывает дескриптор окружения целиком.

Посмотреть, что поймано, можно и без браузера:

```bash
curl -s http://127.0.0.1:8025/api/v1/messages | python3 -m json.tool
```

### Изоляция набора от машины

> **Набор изолирован от машины.** `tests/conftest.py` делает две вещи.
>
> `detached_env` уводит тест в пустой каталог и снимает **всё**, кроме
> явного белого списка: переменные самого прогона, интерпретатора, локали,
> раннера и демона Docker. Это не первая попытка — предыдущие три шли от
> обратного, перечисляя то, что надо убрать, и каждая что-то пропустила:
>
> | Перечисляли | Что пропустили |
> |-------------|----------------|
> | имена переменных | `DATABASE_TYPE`, `DATABASE_NAME`, `DATABASE_HOST`, `DATABASE_USER`, `DATABASE_PASSWORD` — назван был один `DATABASE_URL` |
> | классы конфигурации | `CeleryConfig` с `CELERY_BROKER_TIMEOUT`, живущий вне профилей приложения |
> | дескрипторы `EnvField` | `DATABASE_POOL_SIZE`, `DATABASE_MAX_OVERFLOW`, `DATABASE_POOL_RECYCLE` — `BaseConfig` объявляет их свойствами через `read_env` |
>
> Промах каждый раз был одной формы: конфигурация обзаводилась новым
> способом назвать настройку, о котором перечисление не знало. Белый список
> это заканчивает — новая настройка накрыта в момент появления, каким бы
> идиомом её ни объявили, а дописывать сюда приходится только то, что нужно
> самой машинерии тестов, и оно заявляет о себе сразу, отказом.
>
> Сама защита проверена тестами:
> `tests/unit/infrastructure/test_config/test_env_isolation.py` подсаживает
> настройки фикстурой **модульной** области — то есть до того, как
> отработает `detached_env`, — и убеждается, что до теста не доходит ни одна.
> Выключите чистку, и восемь из десяти тестов там краснеют. Подключается
> модулем целиком:
> `pytestmark = pytest.mark.usefixtures("detached_env")`. Нужен везде, где
> строится профиль, отличный от `testing`, — в том числе когда профиль
> строится побочно, при импорте (так делает `test_task_queue.py`: патч
> задачи Celery финализирует приложение, а `on_configure` собирает
> настоящую конфигурацию).
>
> Автофикстура восстановления `os.environ` работает на всём наборе.
> `ConfigFactory` не просто читает `.env`, а публикует его в окружение
> процесса — иначе ленивым дескрипторам негде взять значения, — и
> `monkeypatch` этого не откатывает, потому что писал не он. Один
> незащищённый тест менял окружение для всех, кто шёл следом.
>
> Замерено до правки: `.env` из одной строки `GUEST_LINK_LIMIT=-7` превращал
> зелёный набор в `5 failed, 1 error`. Одной из жертв был
> `test_production_config_cookie_security`, у которого собственный
> `monkeypatch.chdir` уже стоял: в одиночку он проходил, а за соседом падал.

### CI: набор гоняется дважды

`.github/workflows/tests.yml`. Запускается на каждый push — в любую ветку и
на теги тоже, фильтров нет. Кнопка **Actions → tests → Run workflow**
появится только после того, как файл окажется в ветке по умолчанию
(`master`): ручной запуск GitHub предлагает лишь для workflow, лежащих там.

Бейдж состояния в README живой — он строится по адресу
`actions/workflows/tests.yml/badge.svg`. Заработает он при двух условиях:
файл workflow лежит в ветке по умолчанию **и** репозиторий публичный.
Сейчас репозиторий приватный: анонимный запрос к бейджу отдаёт 404
(проверено), а картинки в README GitHub тянет через свой прокси, без
учётных данных читателя. До открытия репозитория бейдж будет отображаться
битым — это ожидаемо, а не поломка.

Две задачи матрицы идут параллельно и обе всегда досчитывают до конца
(`fail-fast: false`) — «clean прошёл, hostile упал» это и есть диагноз,
отменять половину его нельзя.

| Задача | Окружение | На какой вопрос отвечает |
|--------|-----------|--------------------------|
| `clean` | Свежий чекаут: ни `.env`, ни настроек проекта в окружении | Проходит ли набор сразу после клонирования — до шага 2 быстрого старта, где `.env` появляется? |
| `hostile` | `.env` из `.env.example` **плюс семь дописанных значений**, отличных от умолчаний, + экспортированные `GUEST_LINK_LIMIT=1`, `BATCH_CREATE_LIMIT=2`, `DATABASE_URL=postgresql://prod/real`, `SQLALCHEMY_ECHO=true`, `DATABASE_POOL_SIZE=41`, `DATABASE_MAX_OVERFLOW=43`, `DATABASE_POOL_RECYCLE=47` | Даёт ли тот же результат замусоренная машина разработчика? |

Ни одна из задач не заменяет другую. `clean` проверяет то, что нельзя
проверить у себя: у разработчика `.env` есть всегда. `hostile` проверяет
обратное — что подсунутая конфигурация не меняет ни одного утверждения в
тестах. Чистое окружение этого поймать не может: там нечему утечь. Утечка
была настоящей — тест конфигурации строил профиль `development`,
подхватывал экспортированный `DATABASE_URL`, соединялся с посторонней базой
и дописывал в её таблицу `roles` четыре строки, отчитываясь «2 passed».

> **Чего зелёный `hostile` не доказывает.** Он не означает, что тесты не
> читают `.env` — только что эти четырнадцать значений ничего не сломали.
> Проверено: враждебная задача целиком, запущенная против дерева **без**
> `tests/conftest.py` и без обоих `pytestmark`, даёт зелёный результат.
> Утечка, которая мотивировала всю эту работу, всплывала на **невалидном**
> значении, а все подставляемые здесь валидны. За «ни один тест не читает
> чужую конфигурацию» отвечает изоляция в `tests/conftest.py`, а не эта
> задача; враждебный прогон стережёт соседнюю дверь — что чтение, если оно
> есть, не проходит незамеченным в утверждениях.

Что именно ловит какая переменная — без иллюзий. Громкая пара это
`GUEST_LINK_LIMIT=1` и `BATCH_CREATE_LIMIT=2`: тест, прочитавший чужой
лимит, проваливает обычный `assert`. `DATABASE_URL` ловит только тест,
который дойдёт до **создания движка**; тест, который переменную прочитает
и на неё же проверится, пройдёт молча. Стоит она здесь потому, что
настоящая утечка шла именно через неё.

Срабатывает `DATABASE_URL`, кстати, не так, как выглядит. Имя хоста `prod`
действительно не разрешается, но до DNS дело не доходит: SQLAlchemy
отображает голую схему `postgresql://` на диалект psycopg2, а проект везёт
psycopg 3, и `create_engine()` бросает `ModuleNotFoundError` сразу — до
соединения. Это даже надёжнее и срабатывает раньше: резолвер с search
domain мог бы превратить `prod` во что-нибудь настоящее, а отсутствующий
драйвер не превращается ни во что.

Один только `cp .env.example .env` — инструмент слабый: шаблон в основном
повторяет умолчания профилей. Замерено против `development`: из 71 ключа,
который профиль вообще знает, 60 совпадают дословно, а расходятся всего
пять (`HOST`, `DATABASE_NAME`, `CACHE_LINK_TTL`, `CACHE_STATS_TTL` и
`BASE_URL`, который следует из `HOST`). Остальное, что тест может
прочитать, совпадает с тем, что он увидел бы вообще без файла.

`DATABASE_POOL_SIZE`, `DATABASE_MAX_OVERFLOW` и `DATABASE_POOL_RECYCLE` в
шаблоне тоже отличаются от того, что отдаёт профиль, но расхождение это
бумажное: `_pool_setting` первым делом смотрит `DATABASE_TYPE`, и на
SQLite возвращает `0`, не заглядывая ни в файл, ни в окружение. Поэтому
враждебная задача экспортирует их отдельной переменной — иначе они не
проверяются вовсе.

Поэтому в `.env` дописываются семь строк: `POPULAR_THRESHOLD=7`,
`RECENT_DAYS=3`, `CACHE_LINK_TTL=77`, `CACHE_STATS_TTL=13`,
`MAX_COLLISION_ATTEMPTS=9`, `JWT_ACCESS_TOKEN_EXPIRE_MINUTES=11`,
`DEFAULT_RATE_LIMIT=37`. Все эти настройки профили объявляют — иначе запись
в файл ничего бы не меняла, — но сами **числа** не совпадают ни с одним
умолчанием. Значит тест, увидевший любое из них, видит именно этот файл.
Настоящий `.env` разработчика от шаблона тоже расходится: воспроизводится
это состояние, а не формальное наличие файла.

Кроме двух прогонов workflow делает две отдельные проверки.

**Сверка `requirements.txt` с `uv.lock`** — сразу после установки
зависимостей. `uv sync --locked` сверяет только `pyproject.toml` с
`uv.lock` и на этом останавливается, а `requirements.txt` генерируется из
того же lock и устанавливается в образ через `pip` (`Dockerfile`).
Зависимость, добавленную и залоченную, но не переэкспортированную, не
заметит больше никто: CI тестировал бы один набор пакетов, а образ вёз
другой.

**Число собранных тестов** — перед самим прогоном, по нижней границе
(`MINIMUM_TESTS` в workflow). `--error-for-skips` закрывает дверь с
надписью «skipped», а эта закрывает соседнюю: тест, который не собрался
вовсе, — прогон печатает «N passed» и отдаёт 0 для меньшего N. Механизмы
тихие: файл, выпавший из шаблона `python_files`, функция — из
`python_functions`, `collect_ignore` в каком-нибудь conftest, каталог,
переименованный в то, что pytest пропускает по умолчанию (`norecursedirs`
покрывает `build`, `dist`, `venv`, `node_modules` и всё, начинающееся с
точки: `tests/unit` → `tests/build` даёт молча 760 вместо 2300). Два
несобираемых файла в дереве лежат намеренно — `tests/live/smoke_test.py` и
`tests/live/browser_test.py`.

Переименования в прочие имена и удаление `conftest.py`, который кто-то
импортирует по пути, шумят — но шумят **кодом возврата**, а не числом.
Это вторая причина, по которой код возврата проверяется. Неудобный случай —
`conftest.py`, отдающий только фикстуры: удаление `tests/unit/conftest.py`
оставляет сбор на 2300 и коде 0, и краснеет уже сам прогон.

Граница, а не равенство: добавление тестов правки не требует, удаление —
требует.

Шаг сначала проверяет код возврата `pytest`, и только потом верит числу.
Прерванный сбор печатает то, что успел собрать, и выходит с кодом 2 —
замерено: `3 tests collected, 1 error` там, где тестов было пять. Счёт по
такому выводу отрапортовал бы здоровое число для набора, только что
потерявшего два теста.

#### Как повторить у себя

Точно — только на свежем клоне. В рабочем каталоге лежит ваш `.env`, и
задача `clean` там неповторима по определению. Клонировать проще локально:
приватный репозиторий по HTTPS потребует аутентификации, а результат тот же.

Клон берёт **закоммиченное** состояние: незакоммиченные правки и
неотслеживаемые файлы в него не попадут. Для шагов ниже это значит, что
проверять надо уже закоммиченную работу — иначе клон отстаёт от того, что
у вас на диске.

```bash
git clone . /tmp/ci-check
cd /tmp/ci-check && uv sync --locked

# шаг: requirements.txt против uv.lock
uv export --format requirements-txt --no-hashes --no-emit-project --frozen \
  > /tmp/req.expected
diff -u <(grep -v '^#' requirements.txt) <(grep -v '^#' /tmp/req.expected)

# шаг: число собранных тестов (в CI сверяется с MINIMUM_TESTS).
# Код возврата проверяется до счёта — прерванный сбор печатает
# правдоподобное число и выходит с 2, а конвейер этот код теряет.
listing=$(uv run pytest tests/ -o addopts="" --collect-only -q) \
  && printf '%s\n' "$listing" | grep -c '::' \
  || echo "сбор не завершился — считать нечего"

# clean
uv run pytest tests/ -q --no-cov -o addopts="" \
  --tb=short --error-for-skips

# hostile: .env из шаблона, семь расходящихся значений, затем переменные
cp .env.example .env
cat >> .env <<'EOF'
POPULAR_THRESHOLD=7
RECENT_DAYS=3
CACHE_LINK_TTL=77
CACHE_STATS_TTL=13
MAX_COLLISION_ATTEMPTS=9
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=11
DEFAULT_RATE_LIMIT=37
EOF
GUEST_LINK_LIMIT=1 BATCH_CREATE_LIMIT=2 DATABASE_URL=postgresql://prod/real \
  SQLALCHEMY_ECHO=true DATABASE_POOL_SIZE=41 DATABASE_MAX_OVERFLOW=43 \
  DATABASE_POOL_RECYCLE=47 \
  uv run pytest tests/ -q --no-cov -o addopts="" \
    --tb=short --error-for-skips
```

> **`cp .env.example .env` затирает существующий файл.** В клоне его нет, в
> рабочем каталоге — есть, вместе с вашими секретами. Эта строка только для
> клона.

> **`clean` в клоне чист только по `.env`.** Команда наследует ваш шелл, а
> ничего из него не вычищает. Если у вас экспортированы настройки проекта —
> а документ весь про то, что они экспортированы, — прогон с пометкой
> `clean` таковым не будет. Для честного повтора запускайте его через
> `env -i` с минимальным окружением либо в контейнере.

> **Клон погасит ваш тестовый стек.** Docker-тесты поднимают стек сами и в
> конце делают `docker compose down -v` — а имя проекта у клона то же,
> `link-shortener-test`. Если стек на 5433/6380 сейчас нужен вам, не
> запускайте в клоне полный набор: добавьте `--ignore=tests/integration/docker`.

> **`--error-for-skips` — не украшение.** Флаг (пакет
> `pytest-error-for-skips`) превращает пропуск в отказ. У набора ровно одна
> ветка пропуска — недоступный демон Docker. На ноутбуке это честно: машина
> не может выполнить эти тесты. На раннере, который Docker иметь обязан, это
> дыра — 38 тестов PostgreSQL и Redis исчезают, а прогон остаётся зелёным.
> Проверено откатом: при недоступном демоне без флага `pytest` отдаёт
> `38 skipped` и код возврата 0, с флагом — `38 errors` и код 1.
>
> Чего флаг **не** ловит — перечисление тут важнее утешения. Пропуск,
> случившийся на этапе сбора (`pytest.importorskip`,
> `pytest.skip(allow_module_level=True)`), проходит мимо: флаг цепляется
> только к фазе отчёта о тесте. Такие ловит счётчик собранных тестов.
>
> И `xfail` остаётся живым — вот тут важна деталь, о которую легко
> споткнуться. На уровне отчёта упавший `xfail` **считается пропуском**:
> `rep.skipped` равно `True`, просто рядом выставлен `wasxfail`. Уцелевает
> он только благодаря второму условию внутри плагина — тот спрашивает, было
> ли исключение `pytest.skip.Exception`, а у `xfail` это не оно.
> `-o xfail_strict=true` закрывает половину случая: `xfail`, который начал
> проходить. `xfail`, который продолжает падать, не видит ни один механизм —
> он собирается и он отчитывается. Сегодня в наборе нет ни того, ни другого.
>
> Не поднявшийся тестовый стек — отдельный случай, и флаг тут ни при чём:
> `tests/integration/docker/conftest.py` сам зовёт `pytest.fail`, а не
> `pytest.skip`, для всего, кроме отсутствующего демона. Там и закрыт
> случай `492 passed, 16 skipped`; флаг закрывает соседний.
>
> Сам пакет не поддерживается: версия 2.0.2 выложена в декабре 2019 года,
> внутри 23 строки на старом API `hookwrapper=True`. С pytest 9 работает
> (проверено). Если однажды перестанет — замена умещается в хук
> `pytest_runtest_makereport` в `tests/conftest.py`, файл пока пустой. Но
> писать её надо с обоими условиями: наивное
> `if rep.skipped: rep.outcome = "failed"` завалит каждый `xfail`, ровно по
> причине из абзаца выше.

#### Если CI покраснел не из-за кода

Четыре подозреваемых, все вне кода.

**Медленный `docker info`.** `_docker_is_available()` даёт команде 20 секунд
и ловит `SubprocessError`, куда входит и `TimeoutExpired`, — то есть
превышение бюджета выглядит как «демона нет» и превращается в пропуск. С
`--error-for-skips` этот пропуск становится 38 отказами и красным прогоном,
без повторной попытки. На загруженном раннере с 2–4 vCPU первый `docker
info` может не уложиться. Это цена, которую флаг берёт за закрытую дыру:
молчаливую потерю 38 тестов он больше не допускает, но зато обменивает её на
изредка ложный отказ. Если такое начнёт случаться — поднимать бюджет в
`conftest.py`, а не снимать флаг.

**Лимит анонимных загрузок Docker Hub.** Шаг `docker compose pull` тянет
`postgres:15-alpine` и `redis:7-alpine` без аутентификации, дважды на push
(по разу на задачу матрицы), а раннеры GitHub выходят в сеть через общие
адреса. Упрётся в лимит — покраснеет и шаг загрузки, и, следом,
`compose up` внутри conftest. Лечится добавлением `docker/login-action` с
секретами учётной записи Docker Hub.

**Версия uv не закреплена.** `astral-sh/setup-uv` закреплён точным тегом,
но сам uv он ставит последний доступный: в проекте нет ни
`[tool.uv] required-version`, ни `uv.toml`. То есть инструмент, который
читает `uv.lock` и исполняет `--locked`, меняется между прогонами без
единой правки в репозитории. Лечится входом `version:` у шага установки.

**Тесты с временными допусками на общем железе.** Их двенадцать, и три из
них — однофамильцы, поэтому с путями:

| Файл | Тест |
|------|------|
| `tests/unit/infrastructure/test_health_check.py:202` | `test_the_budget_is_shared_not_granted_to_each_check` — бюджет 0.4 с, проверка `elapsed < 0.9`, запас ровно 0.5 с |
| `tests/unit/infrastructure/test_health_check.py:184` | `test_a_hanging_dependency_does_not_hold_the_answer_open` |
| `tests/unit/infrastructure/test_task_queue.py:159` | `test_concurrent_callers_do_not_each_pay_the_timeout` |
| `tests/unit/infrastructure/test_cache/test_redis_cache_degradation.py:167` | `test_concurrent_callers_do_not_each_pay_the_timeout` |
| `tests/unit/infrastructure/test_rate_limit/test_redis_rate_limiter_degradation.py:213` | `test_concurrent_callers_do_not_each_pay_the_timeout` |
| `tests/integration/docker/test_statement_deadline.py:60` | `test_a_query_past_the_limit_is_aborted` |
| `tests/integration/web/controllers/test_auth_controller.py:229` | `test_unknown_account_is_not_answered_faster` — сравнивает **отношение** двух замеренных задержек, но берёт `min()` из трёх замеров и допускает десятикратную разницу, так что запас у него как раз широкий |
| `tests/unit/infrastructure/test_failover/test_failover_service.py:540` | `test_a_call_in_flight_does_not_hold_up_a_reader` — читатель обязан уложиться в 0.05 с, пока рядом идёт вызов на 0.3 с |
| `tests/unit/infrastructure/test_failover/test_failover_service.py:573` | `test_two_calls_do_not_queue_behind_one_another` — два вызова по 0.2 с обязаны уложиться в 0.3 с на двоих |
| `tests/unit/infrastructure/test_failover/test_failover_service.py:433` | `test_threads_failing_at_once_do_not_skip_a_healthy_standby` — окно 0.02 с на то, чтобы четыре потока успели войти |
| `tests/unit/infrastructure/test_failover/test_failover_service.py:898` | `test_a_climb_is_decided_on_the_list_as_it_is_at_the_time` — проба спит 0.2 с, за это время в неё должен успеть вклиниться вызов |
| `tests/unit/infrastructure/test_logging/test_managers_wire_the_failover_service.py:322` | `test_a_manager_given_an_interval_checks_without_being_asked` — интервал 0.01 с, ждать до 2 с; запас двухсоткратный, и дедлайн достигается только когда поток не стартовал вовсе |

На локальной машине они держатся даже под искусственной нагрузкой
(проверено: прогоны подряд при восьми конкурирующих процессах — ни одного
отказа), но раннер на 2–4 vCPU эти запасы способен съесть. Если такой тест
мигает — это он, а не код.

### Чего набор не держит

Замерено мутационным прогоном 2026-08-10: 90 точечных порч исходного кода,
41 набор поймал, 49 пережили полный прогон.

Все 49 закрыты в тот же день, двумя заходами. Четыре — сразу
(`status_reader` и ширина колонки роли), остальные — тестами, перечисленными
ниже. Способ проверки один и тот же на каждую: порча вносится в копию
дерева, набор гоняется, порча откатывается. Так проверены 39 порч,
покрывающих весь список, — и каждая роняет набор.

| Что переживало набор | Чем закрыто |
|----------------------|-------------|
| Менеджеры отдавали `_active_audit_logger` вместо прокси (при `AUDIT_TYPE=auto` это `None`, то есть 500 на создании ссылки и на переходе по ней); health-checker, всегда отвечающий «здоров»; `check_interval=None`; кулдаун 30 секунд вместо пяти минут | `tests/unit/infrastructure/test_logging/test_managers_wire_the_failover_service.py` |
| `/api/v1/admin/health` проверялся на данных, где все поля равны `True`, поэтому подмена любого поля любым другим была неразличима: упавший Redis показывался здоровым | `tests/unit/web/test_controllers/test_health_body_tells_the_components_apart.py` |
| Соответствие «маршрут → конкретное право» держалось только для пишущих маршрутов; слова `view_system_health` в `tests/` не было вовсе | `tests/integration/web/controllers/test_each_admin_route_asks_for_its_own_permission.py` |
| У админских схем не было отрицательных случаев, кроме пароля и имени роли: снятый шаблон почты, шаблон без якорей, `is_active` по умолчанию `False`, снятое `min_length=1` у `roles` и `permissions` | `tests/unit/web/test_schemas/test_admin_request_contract.py` |
| Привязанные к запросу поля перекрывали одноимённые поля события — аудит записывал бы пользователя из контекста вместо участника события | `tests/unit/infrastructure/test_logging/test_failover_proxies.py` |
| `limit` и `offset` можно было переставить местами: ответ `200` с пустым списком, содержимое не сверял никто | `tests/unit/web/test_controllers/test_admin_api_controller.py` |
| Упавший зонд активного сервиса, отвечающий «работу перенесли»: раунд пропускал подъём, и цепочка не поднималась никогда | `tests/unit/infrastructure/test_failover/test_failover_service.py` |

**Второй круг нашёл шесть мест, которых в списке не было.** Первая версия
этих тестов сама была проверена состязательно — чужим контекстом, которому
поручили ломать прод при зелёном наборе, — и он пробил их в шести местах.
Все шесть закрыты, каждое проверено порчей:

- `admin:manage_users` и `admin:manage_roles` были взаимозаменяемы на
  шести из восьми пишущих маршрутов. Замерено живыми запросами: аккаунт
  только с `admin:manage_roles` заводил учётку (201), деактивировал (200)
  и **удалял** её вместе со ссылками (200);
- читающие маршруты с параметром (`/users/<id>`, `/users/<id>/stats`,
  `/roles/<name>`) переезжали на пишущее право незаметно: матрица держала
  положительную сторону только для трёх маршрутов без параметра. Теперь
  положительная сторона спрашивает про существующие объекты и сверяет
  точный код, а сама матрица покрывает все пять админских прав и все
  маршруты;
- правило адреса, потерявшее `@` в последнем классе, принимало
  `user@example.com@evil.example` — два адреса в одном поле;
- то же правило, записанное как `[^@ \t\n]`, пропускало `\r`, `\v`, `\f`
  и неразрывный пробел, хотя обещало «без пробельных символов»;
- незнакомый `AUDIT_TYPE` (`structlogg`, `syslog`, пустая строка) молча
  выключал аудит: у логгерной цепочки такой тест был, у аудитной — нет,
  и ветка не исполнялась ни одним тестом;
- `rate_limiter` в `GetServiceHealthUseCase` можно было читать из поля
  `database`. Ограничитель, потерявший бэкенд, пропускает всё, включая
  перебор пароля, — и админская панель показывала бы «работает».

**Третий круг вскрыл ещё один пласт — какие объекты стоят в цепочке и что
им передал контейнер.** Логика `FailoverService` оказалась закрыта плотно
(из 86 порч в нём не выжила ни одна семантическая), а незакрытым было
другое:

- **имя, под которым реализация пишет.** Обработчики, поднимающие
  исключение при неудачной записи, ставятся только для «своих» имён
  (`OWN_LOGGER_NAMES`). Реализация, построенная как
  `StructLogger(name="structlog")` или
  `StandardAuditLogger(name="standard_audit")`, теряет это: `ENOSPC`
  снова глотается `handleError`, переключения не происходит,
  `dropped_calls` остаётся нулём. Замерено вдобавок: запись под именем
  `standard_audit` уезжает из `audit.log` в общий лог приложения;
- **что стоит в слоте.** Слот `standard`, построенный `StructLogger`,
  даёт «цепочку из двух реализаций», где обе — одна и та же: отказ,
  снявший основную, снимает и запасную;
- **имя, которое видит оператор.** `LoggerManager.get_active_logger_name`,
  читающий вершину цепочки вместо текущей позиции, делает понижение
  невидимым на единственной поверхности, где оно было видно;
- **шов DI.** `failover_check_interval=None` из компонента; компонент,
  строящий менеджер заново на каждый вызов (поток на событие, состояние
  переключения забыто, health вечно показывает «не запущено»); контейнер,
  отдающий аудиту флаг `LOGGING_ENABLED`; `logging_status`, потерянный по
  дороге в `GetServiceHealthUseCase`; дефолт `FAILOVER_CHECK_INTERVAL`,
  раздутый до пятидесяти минут при проходящей валидации.

Всё перечисленное закрыто: `test_di_hands_the_managers_their_settings.py`,
`test_failover_check_interval.py`, новые классы в
`test_chain_composition.py` и проверка имён в `test_logging_health.py`.

Заодно закрыты две вещи, которых в списке не было.

**Фоновые проверки в наборе никто не останавливал.** Менеджер, построенный
с интервалом, стартует поток-демон в конструкторе; замерено — один каталог
`tests/unit/infrastructure/test_logging/` оставлял к концу сессии двенадцать
живых потоков `_periodic_check`. Теперь тесты состава цепочки строят
менеджеры без интервала, а тем, кому интервал нужен, его гасит фикстура:
после правки живых потоков ноль.

**Адрес с пробельным символом проходил и схему, и объект-значение.**
`[^@]` пропускает перевод строки, поэтому `"user@ex\nample.com"` и
`"user@example.com\n"` принимались обоими; см. [правило адреса
записано дважды](#правило-адреса-записано-дважды).

**Чего этот список не говорит.** Мутационный прогон из 90 порч после этого
не повторялся: проверены поимённо 70 порч — 39 из списка выше, 12 из
второго круга, 11 из третьего и 8 из них же против всего набора, а не
только против своего файла. Новый прогон найдёт новые места — он ручной, и
это его главное ограничение. Круги это и показали: каждая версия тестов
ловила всё, что ей поручали, и не ловила соседнего, пока его не назвали.

Что остаётся слабым и осознанно:

- **Проверка адреса — проверка формы, а не адреса.** Правило требует
  ровно один `@`, точку в правой части и отсутствие пробельных символов.
  `a@b.c` проходит, несуществующий домен проходит. Существование ящика
  теперь проверяется — но не валидацией, а тем, что до перехода по ссылке
  в учётку нельзя войти
- **`MAIL_TIMEOUT` ограничивает одну операцию, а не отправку целиком.**
  Замерено на подставном сервере, отвечающем через 0.8 с на каждую команду
  при `MAIL_TIMEOUT=1`: письмо ушло за 5.7 с, впятеро дольше таймаута, без
  единой ошибки. Порт `Mailer.send` синхронный, поэтому при
  `CELERY_ENABLED=false` это время лежит на потоке запроса. Общего дедлайна
  на отправку нет
- **Ссылка подтверждения — это `GET`, меняющий состояние.** Почтовые
  сканеры, которые ходят по ссылкам (Outlook Safe Links и подобные),
  потратят токен раньше владельца, и владелец увидит «ссылка
  недействительна». Правильный ответ — страница с кнопкой: `GET`
  показывает, `POST` подтверждает. Здесь этого нет
- **Токен подтверждения едет через брокер открытым текстом.** В базе лежит
  только его хэш, отправлять больше нечего — значит при
  `CELERY_ENABLED=true` живой токен лежит в Redis, пока задача ждёт воркера
- **Одноразовость ссылки проверена логикой, но не гонкой.** `claim()`
  решает одним `UPDATE ... WHERE used_at IS NULL`, и тест проверяет, что
  второй вызов не проходит. Двух одновременных запросов тест не ставит:
  под GIL и на SQLite он показал бы не то, что происходит на PostgreSQL
- **`/resend-verification` различим по времени.** Тело и статус там ровные
  с самого начала, а часы — нет: замерено 200 запросами на ветку,
  зарегистрированный адрес отвечает за 0.81 мс против 0.26 мс у
  неизвестного, отношение 3.1, диапазоны не пересекаются. Выравнивать нечем: bcrypt в этом
  пути нет, а работа, которую делает одна ветка, второй просто не нужна —
  остался бы только фиксированный бюджет времени, который под нагрузкой
  начинает врать. Оставлено сознательно: разница другого порядка, чем была
  на регистрации (0.56 мс против 162.52 мс), и тонет в сетевом джиттере, а
  лимит в 3 запроса в час на адрес источника делает набор статистики
  дорогим. Регистрация выровнена — см. [Регистрация не говорит, занят ли
  адрес](#регистрация-не-говорит-занят-ли-адрес)
- **У регистрации остался хвост той же природы.** Свободный адрес пишет две
  строки и коммитит, занятый не пишет ничего, и это единственное, чем ветки
  теперь отличаются по работе. Рядом с bcrypt (~160 мс) и отправкой письма
  (13–15 мс) разница в замере не проступила — отношение медиан 1.0, диапазоны
  перекрываются, — но она есть, и на нагруженной PostgreSQL будет больше,
  чем на SQLite, где мерили. Ни один тест этого не держит: тайминговое
  утверждение в наборе краснело бы от соседнего процесса, а не от правки
- **Слияние учёток, схлопывающихся при нормализации, по-прежнему никто не
  делает.** `flask maintenance normalize-emails` приводит старые адреса к
  нижнему регистру, но пару вида `Clash@example.com` и
  `clash@example.com` оставляет как есть и сообщает о ней: свести две
  учётки в одну — значит решить, чьи ссылки, роли и сессии выживут.
  Команда этого не решает, и ничто другое тоже, так что пара живёт в базе
  сколь угодно долго. Сами по себе записывающие пути её больше не ломают
  (см. следующий пункт), но обе учётки остаются: владелец строки в
  смешанном регистре не может войти — поиск идёт по нормализованному
  адресу и его строку не находит, — а предупреждение о ней уходит в
  журнал при каждом сохранении и не прекратится, пока пару не разведут
  руками
- **Строка в смешанном регистре больше не чинится сама и не роняет
  запись.** Раньше `Email` опускал адрес и на чтении из базы, а
  `_domain_to_orm_fields` писал обратно то, что прочитал: первое же
  сохранение приводило адрес к нижнему регистру мимо команды обслуживания
  и без записи в журнал, а при существующей паре упиралось в уникальный
  индекс — замерено на подтверждении адреса: `IntegrityError`, то есть
  `500`, транзакция откатывалась целиком, токен оставался непотраченным,
  и следующая попытка отвечала так же; админские операции падали тем же
  путём. Теперь чтение восстанавливает строку как есть
  (`Email.from_storage`), запись возвращает её обратно неизменной,
  подтверждение проходит (`200`, токен потрачен), а репозиторий пишет
  предупреждение с именем команды. Держат
  `tests/integration/infrastructure/database/repositories/test_a_row_written_before_normalisation.py`
  и порчи из его докстрингов. См. [Адрес называет одну
  учётку](#адрес-называет-одну-учётку)
- **Что письмо дошло, набор не проверяет.** `TestingConfig` держит
  `MAIL_ENABLED = False` плоским атрибутом, так что отправки в прогоне нет
  вовсе; SMTP-клиент проверен против подставного сервера, а живая отправка
  — только вручную, через Mailpit
- **Юнит-тесты контроллера говорят с моком `AdminService`.** Они держат
  то, что контроллер передаёт вниз и что публикует наверх, и ничего не
  говорят о том, что сервис с этим делает; за это отвечают интеграционные

### Флаги прогона

`-o addopts=""` очищает **весь** список `addopts` из `pyproject.toml`.
Убрать нужно было покрытие, HTML и JUnit — времени стоят, никем здесь не
читаются. Строгость при этом не страдает: она больше не во флагах.

> **`--strict-markers` и `--strict-config` внутри `addopts` не работали.**
> Оба флага — `OverrideIniAction`, а `Config.parse` замораживает список
> переопределений с настоящей командной строки **до** того, как подставит
> `addopts`. Значит копии, лежавшие в `[tool.pytest.ini_options] addopts`,
> до `getini` никогда не доходили и ничего не проверяли. Замерено:
> незарегистрированный маркер под прежними `addopts` давал
> `1 passed, 1 warning`.
>
> Теперь это ini-опции — `strict_markers = true` и `strict_config = true`
> в том же разделе. Так они действуют, и действуют на **любой** запуск, а
> не только на тот, где флаги выписаны заново. Проверено обеими сторонами:
> незарегистрированный маркер прерывает сбор, неизвестный ini-ключ даёт
> `ERROR: Unknown config option` и код возврата 4; уберите ключи — остаются
> одни предупреждения.

Покрытие снимается отдельной командой (см. выше).

`strict_xfail = true` объявлен там же, в ini: тест, помеченный `xfail` и
внезапно начавший проходить, становится отказом, а не строчкой `xpassed`,
которую никто не читает. Каноническое имя ключа именно такое, `xfail_strict`
— его псевдоним.

`--no-cov` после `-o addopts=""` уже ничего не делает — очистка списка
унесла и `--cov`. Оставлен как явное указание намерения: ничего не стоит,
а читателю команды видно, что покрытие здесь не снимается.

## Жизненный цикл гостевой ссылки

1. Гость отправляет POST на `/api/v1/shorten` с URL
2. `CreateShortLinkUseCase` проверяет лимит гостя (`GUEST_LINK_LIMIT` за `GUEST_LINK_WINDOW_DAYS`)
3. Если в пределах лимита, создаёт ссылку с `expires_at = now + DEFAULT_GUEST_TTL_SECONDS`.
   Это потолок, а не только значение по умолчанию: гость, приславший
   больший `ttl_seconds`, получит ровно `DEFAULT_GUEST_TTL_SECONDS`
4. Идентификатор гостя (IP-адрес) сохраняется для rate limiting
5. Ссылка кэшируется в Redis (L1 + L2)
6. После `expires_at` ссылка возвращает ошибку при редиректе

## Удаление гостевой ссылки

У ссылки, созданной без учётной записи, нет владельца, поэтому `link:delete_own`
для неё не срабатывает никогда. Ответ на создание несёт `deletion_token` —
подписанный идентификатор строки, выдаётся единожды. Удалить такую ссылку
можно, передав его в заголовке:

```bash
curl -X DELETE http://localhost:5000/api/v1/links/<code> \
  -H "X-Deletion-Token: <token>"
```

Токен называет конкретную строку, а не код: код освобождается при удалении и
может быть выдан снова.

## Конфигурация

### Профили и .env

`FLASK_ENV` выбирает профиль — класс конфигурации из
`infrastructure/configs/app/`: `development`, `staging`, `production`,
`testing`. Профиль задаёт умолчания, `.env` их переопределяет.

Приоритет, побеждает верхний:

1. настоящая переменная окружения;
2. `.env.<профиль>`;
3. `.env`;
4. умолчание профиля в коде.

Профиль `testing` не читает `.env` — см. `ConfigFactory.NO_DOTENV_ENVS`.

### Как объявляются значения

Поля конфигурации объявляются через хелперы из
`infrastructure/configs/app/env.py`, а не через `os.environ.get()` напрямую:

```python
class BaseConfig:
    GUEST_LINK_LIMIT: int = env_int("GUEST_LINK_LIMIT", 10)
    CORS_ORIGINS: list = env_list("CORS_ORIGINS", ["http://localhost:5000"])
    REDIS_ENABLED: bool = env_bool("REDIS_ENABLED", False)
```

Хелперы возвращают дескриптор, который читает окружение **в момент обращения**
к атрибуту. Прямой вызов `os.environ.get()` в теле класса выполняется при
импорте модуля — то есть до того, как фабрика успевает загрузить `.env`,
и значение из файла молча теряется.

Внутри тел методов и `@property` можно использовать `os.environ.get()`
как обычно: они и так вычисляются лениво.

Доступные хелперы: `env_str`, `env_int`, `env_float`, `env_bool`, `env_list`
(последний разбирает строку через запятую).

Подклассы могут перекрывать поле обычным литералом — например
`TestingConfig.SECRET_KEY = "test-secret-key"`. Обычный атрибут перекрывает
дескриптор, и значение перестаёт зависеть от окружения; для тестов это нужное
поведение.

### Важнейшие команды

Шпаргалка. Подробности по каждой — в разделах ниже и в
[OPERATIONS_AND_MIGRATIONS.md](OPERATIONS_AND_MIGRATIONS.md).

```bash
uv sync                                  # зависимости
uv run flask db migrate                  # схема базы
uv run flask db load-base-roles          # системные роли из roles.yaml
uv run flask create-admin --email a@b.co --password '...'
uv run flask run                         # запуск для разработки
uv run flask security check-secrets      # проверка, что ключи не дефолтные
uv run pytest tests/ -q --no-cov -o addopts=""   # набор
```

### Ключевые настройки

Полный список — в `.env.example`.

| Настройка | По умолчанию | Описание |
|-----------|--------------|----------|
| `GUEST_LINK_LIMIT` | 10 | Макс. гостевых ссылок за окно |
| `GUEST_LINK_WINDOW_DAYS` | 1 | Окно rate limit |
| `DEFAULT_GUEST_TTL_SECONDS` | 604800 | Время жизни гостевых ссылок (7 дней) — и значение по умолчанию, и потолок |
| `MAX_TTL_SECONDS` | 315360000 | Максимальный срок жизни ссылки для любого вызывающего (10 лет) |
| `MAX_URL_LENGTH` | 2048 | Максимальная длина исходного URL; выше не поднять — ширина колонки |
| `ALLOW_INTERNAL_TARGETS` | false | Разрешить ссылки внутрь собственной сети (петля, приватные диапазоны, метаданные облака) |
| `CACHE_LINK_TTL` | 3600 (20 в development) | TTL кэша ссылок (секунды) |
| `CACHE_STATS_TTL` | 300 (20 в development) | TTL кэша статистики (секунды) |
| `COOKIE_SECURE` | false | Secure-флаг для cookie (true в production) |
| `TRUSTED_PROXIES` | (пусто) | Список доверенных прокси через запятую |
| `CORS_ORIGINS` | http://localhost:5000 | Разрешённые origins для CORS |
| `SQLALCHEMY_ECHO` | false | Логирование SQL-запросов (true только для dev) |

### Как запускать

Три способа, и они отличаются не только командой.

| Способ | Команда | Что берёт конфигурацией |
|--------|---------|-------------------------|
| Разработка | `uv run flask run` | `FLASK_ENV` или `development`; `.env` из корня; SQLite файлом |
| Docker | `docker compose --env-file .env.docker up -d --build` | файл из `ENV_FILE`, по умолчанию `.env`; PostgreSQL, Redis и Celery поднимаются рядом; `app` ждёт сервис `migrations` с `condition: service_completed_successfully`, то есть схема применена до старта приложения |
| Продакшен-образ | `CMD` образа | gunicorn с `--worker-class sync`: `--timeout` гарантированно снимает зависший запрос только на sync-воркерах, на `gthread` потолок был бы декорацией |

Пошагово оба сценария разобраны в [QUICKSTART.md](QUICKSTART.md).

### Справочник переменных окружения

`.env.example` — шаблон со всеми переменными и описаниями, единственный
env-файл в репозитории. `.env` и `.env.docker` создаются локально и в git не
попадают. Значения по группам, с диапазонами и последствиями, — в
[OPERATIONS_AND_MIGRATIONS.md](OPERATIONS_AND_MIGRATIONS.md#справочник-конфигурации).

### Безопасность

- JWT токены содержат `type` claim ("access"/"refresh") для предотвращения abuse
- Авторизация только через `Authorization: Bearer <token>` header
- `X-Forwarded-For` читается, только если запрос пришёл с адреса из
  `TRUSTED_PROXIES`, и берётся **крайний правый** элемент — тот, который
  дописал сам прокси и который клиент подделать не может. Значение обязано
  быть IP-адресом, иначе берётся адрес соединения
- Сокращать ссылки внутрь собственной сети нельзя: блок-лист приватных,
  loopback и link-local диапазонов применяется к **числовому значению
  присланного написания**, а не к его буквальной форме, поэтому
  `0177.0.0.1`, `127.1`, `2130706433` и `１２７．０．０．１` распознаются как
  петля. Юзеринфо в URL запрещено: `http://good.example@evil.example/`
  показывает жертве один домен, а ведёт на другой

  > **Чего эта проверка не делает.** DNS не разрешается — ни здесь, ни
  > позже. Имя, которое резолвится во внутренний адрес, проходит:
  > `http://127.0.0.1.nip.io/` принимается (проверено, `201 Created`).
  > Закрыть это разбором имени нельзя в принципе — запись DNS меняется
  > после проверки, — и настоящая защита от этого лежит на сетевом уровне,
  > а не в приложении. Формулировка «адрес, который получит резолвер»
  > стояла здесь раньше и обещала больше, чем есть; докстринг
  > `OriginalUrl` был точен всегда
- CORS ограничен `CORS_ORIGINS` (по умолчанию только localhost)
- `.env` файл не попадает в Docker image (секреты передаются через runtime env vars)

## CLI-команды

```bash
# Ссылки
flask link create --url <url>
flask link create --url <url> --code <code>   # 6-10 of A-Z a-z 0-9 _ -
flask link info <short_code>
flask link delete <short_code>
flask link list --limit 10

# Пользователи
flask create-admin --email admin@example.com --password secret
flask create-user --email user@example.com --password user123 --role user

# Безопасность
flask security check-secrets
flask security generate-secrets
flask security list-users
flask security list-roles
flask security validate-token <jwt_token>

# База данных
flask db check
flask db status
flask db migrate
flask db load-base-roles
flask db seed --count 10

# Alembic
flask alembic status
flask alembic history
flask alembic upgrade head

# Статистика и обслуживание
flask stats show
flask maintenance health
flask maintenance normalize-emails            # покажет; --apply запишет
flask cache clear
```

## Ключевые файлы

| Файл | Назначение |
|------|------------|
| `web/app_factory.py` | Фабрика приложения Flask, связывает всё вместе |
| `infrastructure/di/container.py` | DI-контейнер, lazy-создание всех компонентов |
| `infrastructure/di/components/` | Компоненты DI (cache, database, auth, use_cases) |
| `infrastructure/database/role_loader.py` | Загрузка RBAC из YAML в базу данных |
| `infrastructure/configs/app/base.py` | Все константы конфигурации с переопределением через env |
| `infrastructure/configs/app/env.py` | Дескрипторы ленивого чтения env (`env_str`, `env_int`, ...) |
| `infrastructure/configs/app/factory.py` | Выбор профиля и загрузка `.env`-файлов |
| `migrations/versions/0001_initial_schema.py` | Baseline-миграция: вся схема с нуля |
| `web/controllers/api_controller.py` | REST API эндпоинты |
| `web/controllers/auth_controller.py` | Auth эндпоинты (login, register, refresh, logout) |
| `web/controllers/admin_api_controller.py` | Admin API эндпоинты |
| `web/controllers/dashboard_controller.py` | HTML-страницы панели управления |
| `domain/entities/link.py` | Основная сущность Link с бизнес-правилами |
| `web/middleware/rate_limit.py` | Rate limiting middleware |
| `web/middleware/authentication.py` | Разбор токена (заголовок или cookie), загрузка `g.current_user` |
| `web/middleware/csrf.py` | CSRF-защита cookie-сессий (double-submit) |

## Документация API

- `GET /api/openapi.json` — документ OpenAPI 3.1. Тела запросов и ответов
  генерируются из тех же pydantic-моделей, по которым валидируют эндпоинты.
  Направьте на него Swagger UI, Redoc, Postman или генератор клиента.
- `GET /api/docs` — тот же документ страницей. Просмотрщик не встраивается:
  это полтора мегабайта чужих ассетов или тег `script` на чужой CDN.

Тест `tests/integration/web/controllers/test_api_docs.py` сверяет документ с
настоящей картой маршрутов, поэтому новый эндпоинт — это падающий тест, а не
незадокументированный эндпоинт.

> **Сквозные ответы не пишутся по операциям, а добавляются поверх всех.**
> Два слоя стоят перед всем приложением: троттл может ответить `429` на
> любой запрос, CSRF — `403` на любой небезопасный запрос, **который
> аутентифицирован cookie** (клиент с валидным `Authorization: Bearer` и
> без cookie не получает `403` ни на одной операции; исключение —
> `/auth/refresh` и `/auth/logout`, они читают cookie сами). `429` до
> правки объявляли четыре операции из тринадцати, а `403` — три, и те про
> нехватку прав, а не про CSRF: у сгенерированного клиента не было ветки
> на отказ, который приходит раньше любого эндпоинта. OpenAPI 3.x не умеет объявить ответ один раз на
> весь документ: просьба
> ([OAI/OpenAPI-Specification#521](https://github.com/OAI/OpenAPI-Specification/issues/521))
> прожила с 2015 года до закрытия в марте 2024 и перенесена в следующую
> версию, а для 3.x ответ по-прежнему «сошлитесь на него из каждой
> операции». Вписывать руками в тринадцать операций — ровно тот способ,
> которым документ отстаёт от кода, поэтому `_add_cross_cutting_responses`
> добавляет их программно, а
> «безопасные» методы объявлены здесь вторым списком намеренно: документ
> описывает приложение и не участвует в нём, а импорт из `web/middleware`
> в `web/schemas` был бы первым ребром в эту сторону — сегодня все рёбра
> идут обратно. Цена второго списка в том, что он может разойтись, поэтому
> равенство держит тест. Отдельным тестом проверяется и то, что объявленный
> `403` приложение действительно отдаёт: проверка ключей в документе не
> может упасть, пока генератор работает, и эндпоинт, тихо выведенный
> из-под CSRF-слоя, ловится только живым запросом.

## Как добавить новый use case

1. Создайте `application/use_cases/<domain>/<name>.py` с `@dataclass`, наследующим `BaseUseCase`
2. Добавьте порт (интерфейс) в `application/ports/`, если нужна новая инфраструктура
3. Свяжите в `infrastructure/di/components/use_cases/`
4. Добавьте геттер в `infrastructure/di/container.py`
5. Экспортируйте через фасад-сервис или напрямую в контроллере
6. Напишите тесты в `tests/unit/application/test_use_cases/`

## Принятые решения и границы

Каждая запись — место, где поведение выбрано, а не получилось само. Найдено
аудитом, воспроизведено, обсуждено и оставлено таким намеренно. Здесь они
затем, чтобы через полгода не всплыть как новость и не быть «починенными»
обратно.

### Вне `development` разрешён только PostgreSQL

**Решено** (2026-08-11): `production` и `staging` отказываются стартовать
на любом бэкенде, кроме PostgreSQL; `development` берёт и SQLite, и
PostgreSQL; профиль, который никто не назвал, остаётся `development` и
стартует на SQLite.

**Почему.** `DATABASE_TYPE` по умолчанию `sqlite`, а `DATABASE_NAME` —
`db_shortener`, поэтому развёртывание, забывшее задать базу, поднималось
на пустом новом файле и отвечало так, будто данных никогда не было.
Замерено: `production` со всем остальным заданным проходил проверку и
открывал `sqlite:////<корень>/db_shortener`, `/health` отвечал
`healthy`, а первый же запрос — 500 `no such table: roles`.

Проверяется бэкенд, а не забытая настройка, потому что к SQLite ведёт
несколько дорог и каждая кончается одинаково: неназванный файл создаётся
пустым; относительный путь в `DATABASE_URL` идёт за рабочим каталогом
процесса, а он у `gunicorn`, `celery` и `flask` разный (замерено —
миграция записала 147456 байт в один файл, приложение открыло пустой
рядом); база в памяти под `SingletonThreadPool` выдаёт каждому потоку
свою (форма `file::memory:?cache=shared&uri=true` — исключение, там
`QueuePool` и общая база, но она всё равно исчезает с процессом).

**Что осталось открытым и почему.** Неназванный профиль не отвергается:
умолчание `development` одинаково на хосте и в стеке, так что отказ
достался бы разработчику, который ничего не настраивал намеренно.
Следствие — `FLASK_ENV`, записанный только в `.env.<профиль>`, профиль не
выбирает (файл читается уже после выбора), и развёртывание, настроенное
так, проверку не увидит.

### Правило адреса записано дважды

**Решено** (2026-08-10): держать два написания одного правила —
`EMAIL_PATTERN` для схемы и `EMAIL_PATTERN_RE` для домена — и стеречь их
согласие тестом на адресах, а не на выражениях.

**Почему.** Проверяют адрес два разных движка регулярных выражений. Схему
проверяет Rust-библиотека `regex` через pydantic, объект-значение —
`re` самого Python, и якорь конца строки у них разный: Python документирует
`$` как совпадающий «at the end of the string or just before the newline at
the end of the string», Rust — как конец текста. Одно выражение на обоих не
даёт одного правила: с `$` домен принимал бы `"user@example.com\n"`, а
`\Z`, который это чинит, Rust-движок не понимает вовсе — замерено,
`unrecognized escape sequence` при построении схемы.

Прежнее выражение `^[^@]+@[^@]+\.[^@]+$` стояло в обоих местах отдельными
копиями и пропускало пробельные символы: `[^@]` включает перевод строки,
поэтому `"user@ex\nample.com"` и `"user@example.com\n"` принимались и
схемой, и доменом. Адрес попадает в каждую строку журнала и каждую запись
аудита об учётной записи, а с появлением почтового канала попадёт в
заголовок письма, где перевод строки — это то, чем пишется инъекция
заголовка.

**Держит** `TestTheSchemaAndTheDomainAgreeOnAnAddress`: один и тот же
список адресов подаётся обоим валидаторам, и вердикты обязаны совпасть.
Сравнение самих выражений держало бы только константу против себя.

### `mask_url` не трогает учётные данные в адресе без `://`

**Решено** (2026-08-10): шаблон не расширять, границу зафиксировать тестом.

**Почему.** Адрес без схемы (`user:pass@example.com/x`) и адрес со схемой без
`//` (`mailto:`, `data:`, `javascript:`) authority не имеют вовсе, и
`urlsplit` её не находит. Попасть в хранилище такое значение не может:
`_validate_scheme` отбивает `user:` и `mailto:` как схему на входе.
Расширение шаблона на произвольный текст стоило бы ложных срабатываний на
адресах, которые ничего не скрывают.

**Держит** `test_credentials_with_no_scheme_at_all_still_get_through`.

### `mask_url` не трогает вложенный адрес в percent-encoding

**Решено** (2026-08-10): шаблон не расширять.

**Почему.** Шаблон ищет `scheme://userinfo@` в исходной строке, поэтому
`?next=https://al:s3cret@evil.example/` вычищается, а
`?next=https%3A%2F%2Fal%3As3cret%40evil.example%2F` — нет. Замерено
мутационным прогоном: из четырёх закодированных форм (только `%40`, только
`%2F%2F`, полностью закодированная, вложенная без схемы) не маскируется ни
одна, и все четыре проходят валидатор — то есть попадают в аудит как есть.
Именно такую строку даёт `urlencode`, так что это обычный вид
параметра-редиректа, а не экзотика. Чинится декодированием query перед
поиском — но декодирование меняет строку, которую аудит обязан записать в том
виде, в каком она пришла, а частичное кодирование даёт много написаний одного
и того же.

### `mask_url` не трогает токен в query-строке

**Решено** (2026-08-09): вырезать только userinfo, про query сказать прямо.

**Почему.** Учётные данные перед хостом убираются
(`https://user:pass@host/` → `https://***@host/`), а `?token=...` попадает в
аудит-журнал как есть. Вычищать и его — значит вести список имён параметров, а
он никогда не полон: `token`, `key`, `access_token`, `sig`, `code`, имя из
чужого API. Видимость полной очистки хуже честной границы.

**Держит** `test_a_token_in_the_query_still_gets_through`.

### Неаутентифицированное удаление выдаёт, занят ли короткий код

**Решено** (2026-08-10): признать приемлемым, статусы не выравнивать.

**Почему.** `DELETE` без входа на существующую ссылку отвечает `401`, на
несуществующую — `404`, то есть перебором можно узнать, какие коды заняты. Но
эта же информация публична по построению сервиса: `GET /<code>` отвечает `302`
на занятый код и `404` на свободный, без всякой аутентификации. Выравнивание
статусов в `DELETE` не закрыло бы ничего, а стоило бы либо ответа `401` на
заведомо несуществующий код (против принципа «одно условие — один статус» из
`TestAStatusThatMatchesWhatHappened`), либо `404` анониму, который забыл
`deletion_token` от собственной ссылки и не поймёт, что дело в токене.

**Держат** `test_guest_link_deletion.py` и `test_request_hardening.py`: обе
ветки теперь закреплены отдельно, поэтому правка, отвечающая `401` всегда,
набор сломает. Раньше оба теста принимали любой из двух кодов.

### Фоновая проверка держит блокировку на время health-пробы

**Решено** (2026-08-10): оставить как есть.

**Почему.** `_attempt_upgrade` и `_attempt_demotion` берут блокировку на весь
обход списка вместе с пробами, а `StandardLogger.is_healthy` пишет
debug-запись, то есть ходит на диск. Раз в `check_interval` (30 с по
умолчанию) запись лога может подождать пробу. Просто вынести пробу наружу
нельзя: вердикт тогда строится по индексу, который упавший вызов успевает
сдвинуть, и понижение затирается ответом старше себя — воспроизведено.
Проба — это `is_healthy` двух логгеров, то есть десятки микросекунд раз в
30 секунд; сужение требует перепроверки индекса под блокировкой и отдельного
решения о том, что делать с ответом про сервис, который за время пробы успели
сменить. Риск внести гонку в самую тонкую часть класса выше выгоды.

**Держат** `test_a_climb_is_decided_on_the_list_as_it_is_at_the_time` и
`tests/unit/infrastructure/test_failover/test_lock_boundaries.py`, который
инструментирует саму блокировку. Замерено на девяти мутациях: семь падают, две
живут — разбиение одного захвата на два вокруг той же локальной переменной
(поведения не меняет) и вынос инкремента `failed_checks` из-под блокировки (на
CPython одиночный `+= 1` не теряется, и наблюдать нечего).

### `ALL_SERVICES_FAILED` не смотрит ни один вызывающий

**Решено** (2026-08-10): оставить как есть.

**Почему.** Прокси в `logger_manager.py` и `audit_manager.py` результат
`execute` выбрасывают: все записывающие методы объявлены `-> None`, поэтому
различить сентинел мог бы только тот, кто смотрит возвращённое значение.
Исходная проблема — «аудит перестал писаться» выглядит как «записалось
нормально» — закрыта с другой стороны: `dropped_calls`, `failed_checks` и
`lost_log_lines` отдаёт `GET /api/v1/admin/health` (секция `logging`), то есть
потеря видна наблюдателю, не спрашивая вызывающего. Поднимать исключение из
прокси означало бы ронять пользовательский запрос из-за сбоя журнала, что
противоречит замыслу всего механизма.

### Разделяемая in-memory база задаётся только через `DATABASE_URL`

**Решено** (2026-08-10): `DATABASE_NAME` с `?` отвергать на `validate()`.

**Почему.** По документации SQLAlchemy рабочая форма —
`sqlite:///file::memory:?cache=shared&uri=true`, и без `uri=true` это файл по
определению (замерено: на диске появился файл с именем `file::memory:`).
`DATABASE_URL` обходит привязку к корню целиком, поэтому тот, кому нужна
разделяемая память, пишет её туда.

### Сбой собственного логгера `FailoverService` считается, а не пересказывается

**Решено** (2026-08-10): `_say` пишет в логгер, при отказе — в stderr, при
втором отказе — молчит, и в любом случае считает строку на
`lost_log_lines`.

**Почему.** Раньше `self.logger` звался напрямую, и логгер, бросивший
`OSError(ENOSPC)`, выносил исключение из `execute` в пользовательский запрос —
из класса, существующего ровно затем, чтобы сбой журналирования до
вызывающего не доходил. Замерено до правки: исключение уходило наружу, а
`dropped_calls` оставался нулём, то есть потеря не была ни поглощена, ни
сосчитана. Порядок «логгер → stderr → молчание» взят у стандартной библиотеки:
`Handler.handleError` пишет сбой в `sys.stderr` и держит весь свой
диагностический блок, вместе с этой записью, под `except OSError: pass`,
потому что диск, уронивший логгер, обычно роняет и stderr. Счётчик считает
отказы логгера, а не «нигде не записанные» строки: куда направлен stderr —
дело развёртывания, а отказ — то, что сервис наблюдает сам.

**Держит** класс `TestALoggerThatThrowsIsNotTheCallersProblem`. Замерено
мутациями: снятие защиты, снятие счётчика, снятие stderr-запасного, замена
`except Exception` на `except BaseException` и потеря `return` после удачной
записи — каждая роняет набор. Дорогу счётчика до `/health` держит
`test_status_reader.py`: до него `ComponentLoggingStatus` не проверял никто, и
перестановка цепочек местами проходила незамеченной — замерено.

### Имя роли ограничено набором символов, а не только длиной

**Решено** (2026-08-10): `^[A-Za-z0-9_-]+$`, длина 2–50, описание до 255.

**Почему.** Имя роли — последний сегмент URL, через который проходит каждый
маршрут над одной ролью. Замерено против живых маршрутов со снятым шаблоном:
`role/with/slash` и `../admin` создаются с `201`, а `GET` и `DELETE` на них
отвечают `404` — стандартный конвертер Werkzeug берёт один сегмент пути.
Такая роль оставалась в базе навсегда: ни адресовать, ни удалить иначе как
SQL-запросом.

Ломает маршрут только слэш. Остальные имена набора — `bad\nname`,
`role with space`, имя из двух пробелов, `..` и `%2e%2e` — в том же замере
создаются и удаляются с `200`, и отвергнуты по другим основаниям: перевод
строки уезжает в каждую строку журнала о роли, имя из пробелов неотличимо в
списке, а `..` читается как обход у любого, кто склеит его с путём.

Границы длины взяты по ширине колонок: замерено на PostgreSQL 15, что
51 символ в `VARCHAR(50)` и 256 в `VARCHAR(255)` дают
`StringDataRightTruncation`, то есть `500` вместо ответа, называющего поле.
SQLite ширину не проверяет, поэтому набор об этом молчал.

**Цена.** Контракт сузился: `POST /api/v1/admin/roles` с таким именем теперь
отвечает `400` вместо `201`. Существующие роли не затронуты — схема
валидирует только создание, а чтение и назначение идут мимо неё.

**Держат** `TestARoleNameIsBoundedByCharacters` и
`TestTheWidthsAgreeWithTheMigratedDatabase`. Второй читает ширину из
`migrations/versions/0001_initial_schema.py`, а не из ORM-модели: модель
строится из той же константы, поэтому сравнение с ней двигало бы обе стороны
разом. Замерено: `ROLE_NAME_MAX_LENGTH = 80` оставляет остальные тесты файла
зелёными, пока миграция по-прежнему создаёт `VARCHAR(50)`.

### Регистрация не говорит, занят ли адрес

**Решено** (2026-08-10): один ответ `202` на оба исхода, тело без учётки,
письмо на занятый адрес, хеширование пароля до проверки существования.
Админский путь оставлен как был.

**Почему.** Раньше занятый адрес отвечал `400` и «Email already registered»,
то есть прямо сообщал анониму, зарегистрирован ли адрес. OWASP Authentication
Cheat Sheet, раздел *Account creation*, перечисляет ровно это среди
неправильных ответов («This user ID is already in use.») — вместе с
«Welcome! You have signed up successfully.», поэтому одного обезличивания
текста было мало: правильный ответ там один и тот же для обоих исходов
(«A link to activate your account has been emailed to the address
provided.»). ASVS 5.0 требует того же в 6.3.8 — уровень L3, не L1:
«Verify that valid users cannot be deduced from failed authentication
challenges, such as by basing on error messages, HTTP response codes, or
different response times. Registration and forgot password functionality
must also have this protection.»

Тело и статус — не весь ответ. Замерено 25 запросами на каждую ветку до
правки: занятый адрес отвечал за 0.56 мс, свободный — за 162.52 мс,
разница в 290 раз, диапазоны (максимум 11.0 мс против минимума 162.0 мс) не
пересекались. Перечисление не требовало статистики: хватало одного запроса.
Причина — «quick exit»: проверка существования замыкалась до bcrypt. Пароль
теперь хешируется **до** обращения к базе, что и есть вторая форма из
псевдокода OWASP. После правки: 160.70 мс против 161.38 мс, отношение 1.0,
диапазоны перекрываются целиком.

Письмо на занятый адрес — не вежливость, а вторая половина того же замера.
При `CELERY_ENABLED=false` (умолчание) отправка идёт синхронно на потоке
запроса, и ветка, которая ничего не отправляет, короче на длину SMTP-обмена:
замерено против Mailpit на петле — 13–15 мс, удалённый релей дороже. WSTG
предупреждает ровно об этом в *Analyzing Response Times*: обращение к
внешней службе (отправка письма) добавляет сотни миллисекунд и само
становится признаком. С письмом обе ветки против Mailpit дают 175.26 мс
против 175.00 мс.

Форма подтверждения делает обезличивание честным, а не показным. Об этом
же пишет django-allauth в описании `ACCOUNT_PREVENT_ENUMERATION`: «In case
of mandatory verification, enumeration can be properly prevented because the
case where an email address is already taken is indistinguishable from the
case where it is not.» У нас подтверждение обязательно, поэтому второй
учётки на тот же адрес не заводится и врать не приходится.

**Цена.**

- Контракт сломан разом, без переходного периода: `201` + `user` стали
  `202` + одно `message`. Клиент, которому нужен идентификатор, входит за
  ним. Флаг совместимости не заводился намеренно — он стал бы вторым
  `RATE_LIMIT_AUTH_DISABLED`, то есть выключателем защиты, который однажды
  забудут вернуть;
- страница регистрации больше не перебрасывает на `/login`, а показывает
  ответ сервиса: перебросить — значит сказать «получилось», а это ровно то
  утверждение, которое делать нельзя;
- на занятый адрес уходит письмо, которого его владелец не просил. Просил
  его тот, кто ввёл адрес в форму, и письмо об этом и сообщает. Рассылку
  ограничивает тот же лимит, что и регистрацию, — 3 запроса в час на адрес
  источника;
- письмо не может предложить восстановление пароля, потому что
  восстановления пароля, которым человек воспользуется сам, в сервисе нет:
  сбросить пароль может только оператор командой
  `flask security reset-password`, и звать в письме к незнакомому адресату
  некуда. Оно зовёт на `/login` и только. Человеку, который не завершил
  регистрацию и потерял письмо, оно не помогает: запросить ссылку заново
  можно лишь вызовом `POST /api/v1/auth/resend-verification`, на странице
  входа такой кнопки нет;
- админский путь (`UserManagementService.create_user`) по-прежнему отвечает
  «Email already registered». Раскрытие там уместно: вызывающий
  аутентифицирован и имеет `admin:manage_users`, а требование OWASP — про
  анонима. Обезличивание отняло бы у администратора полезное сообщение и не
  закрыло бы ничего.

**Держат** три файла, каждый — свой слой.

`tests/unit/application/test_use_cases/test_registration_is_anonymous.py`
держит **работу**, а не ответ: обе ветки хешируют ровно один раз и отдают
очереди ровно одно письмо. Там же закреплён порядок — уведомление уходит
после закрытия блока `with`, потому что без брокера передача и есть
SMTP-обмен, а сделанный внутри он держал бы соединение с базой на время
сетевого вызова. Первая версия правки именно это и делала.

`tests/integration/web/controllers/test_registration_does_not_say_who_is_registered.py`
сверяет две ветки **друг с другом**, а не с литералом: правка, сдвигающая
обе разом, всё ещё неразличима, и проверка против зашитого `202` этого не
сказала бы. Отдельным тестом пришит сам успешный статус — иначе
«одинаково» прошло бы и на одинаковых `500`.

`tests/integration/web/controllers/test_account_exists_notice_goes_out.py`
держит **проводку**: с `CELERY_ENABLED=false` письмо уходит через
синхронный запасной путь, который ставит контейнер одной строкой. Убрать
её — и уведомление не отправляется, ветки перестают стоить одинаково, а
все проверки с моками остаются зелёными: очередь-двойник по-прежнему
«принимает» письмо. Тест ходит через ту же проводку, а не мимо неё.

Проверено восемью порчами: возврат «quick exit», отказ от письма, `400` на
занятый адрес, учётка в теле ответа, ссылка подтверждения на чужой адрес,
одинаковый `500` в обеих ветках, передача письма внутри открытой
транзакции и снятая проводка в контейнере. Каждая роняет набор, и каждая
роняет тот тест, который её обещает ловить.

### Адрес называет одну учётку

**Решено** (2026-08-10): `Email` держит адрес в нижнем регистре целиком —
и локальную часть, и домен. Пробелы по-прежнему не срезаются.

**Почему.** `find_by_email` сравнивает строку, и уникальный индекс на
`users.email` сравнивает её же, поэтому регистр решал, одна это учётка или
две. Замерено живыми запросами до правки: после регистрации
`case@example.com` запрос с `Case@Example.com` отвечал теми же `202` и
заводил **вторую учётку** — две строки на один почтовый ящик, два письма
подтверждения, и вход, который работал или не работал в зависимости от
того, как адрес написали в первый раз.

Стандарт делит адрес пополам, и правка это делит намеренно не так. RFC
5321, раздел 2.4: домены «follow normal DNS rules and are hence not case
sensitive» — эту половину опускать безопасно. Вторую он оставляет за
принимающим хостом: «The local-part of a mailbox MUST BE treated as case
sensitive. Therefore, SMTP implementations MUST take care to preserve the
case of mailbox local-parts». То есть сервер, который различает `Smith` и
`smith`, получит письмо не для того пользователя. Тот же абзац
заканчивается словами «However, exploiting the case sensitivity of mailbox
local-parts impedes interoperability and is discouraged», и ни один
почтовый провайдер в обиходе так не делает — это и есть сделка: одна
учётка на ящик ценой правила, которое стандарт отдаёт получателю.

Django опускает только домен (`normalize_email` — «Normalize the email
address by lowercasing the domain part of it») и тем самым сохраняет ту
самую двусмысленность, ради которой всё это делалось. Поэтому здесь
опускаются обе половины.

**Цена.**

- Адрес в базе больше не тот, что напечатал человек. Письмо уходит на
  нормализованный, потому что именно он совпадает со строкой учётки;
- строки, записанные до правки, перестают находиться: `Case@Example.com` в
  базе не отвечает на поиск `Case@Example.com`, потому что искать будут
  `case@example.com`. Для них есть `flask maintenance normalize-emails` —
  по умолчанию только отчёт, `--apply` записывает;
- пара, схлопывающаяся в один адрес, командой не трогается и попадает в
  отчёт. Слияние — решение владельца данных, не миграции. Конфликтом
  считается совпадение **опущенных** форм с обеих сторон: первая версия
  спрашивала, занято ли уже нижнее написание, и потому не видела пару
  `Case@Example.com` / `CASE@Example.com` — обе объявлялись безопасными,
  запись падала на уникальном индексе, и вся правка откатывалась, оставляя
  без миграции и все посторонние адреса. Теперь каждый адрес пишется в
  своей транзакции, поэтому отказ по одному не уносит остальные;
- **нормализация — на входе, и только там.** `Email` опускает регистр
  того, что ему передали, а `Email.from_storage` восстанавливает строку
  из базы как есть: ряд, прочитанный из колонки, — это не ввод, а
  результат прежнего ввода, и опускать его второй раз значит менять
  данные, а не чистить их. Пока чтение тоже нормализовало, любое
  сохранение старой строки переписывало её — мимо команды выше и мимо
  журнала, — а при существующей паре упиралось в уникальный индекс.
  Замерено на подтверждении адреса: `IntegrityError`, ответ 500, токен
  непотрачен, следующая попытка падает так же, то есть подтвердить
  учётку нельзя никогда; деактивация администратором падала тем же
  образом. Теперь запись возвращает в колонку ровно то, что из неё
  прочитали, и обе беды исчезают;
- само правило нижнего регистра живёт в одном месте — `Email.normalise`.
  Его спрашивают и репозиторий (чтобы понять, нормализована ли строка),
  и вывод команды обслуживания, вместо того чтобы звать `lower()`
  каждый у себя;
- строку, которая всё же не нормализована, репозиторий сопровождает
  предупреждением с именем команды — на каждое сохранение, потому что
  строка переживает запрос. Для **пары** команда ничего не сделает: она
  такие строки отвергает по замыслу, и предупреждение будет идти, пока
  пару не разведут руками;
- пробелы не срезаются намеренно: `strip()` — очевидный спутник `lower()`,
  и он молча начал бы принимать хвостовой перевод строки, который правило
  адреса отвергает нарочно. Адрес уезжает в почтовый заголовок, а перевод
  строки в заголовке — это инъекция.

**Держат** `tests/unit/domain/test_value_objects/test_email_identity.py`,
`tests/integration/web/controllers/test_one_mailbox_is_one_account.py`,
`tests/integration/cli/test_normalize_emails_command.py` и
`tests/integration/infrastructure/database/repositories/test_a_row_written_before_normalisation.py`.
Последний держит пункт про сохранение: что строка не переписывается, что
о ней предупреждают с именем команды, что обычная учётка не
предупреждает, и что пара, которая раньше давала 500, теперь
подтверждается. Второй проходит
весь путь по HTTP — регистрация, повторная регистрация другим регистром,
вход тремя написаниями, — потому что нормализация в объекте-значении
ничего не стоит, если её теряет какой-нибудь слой между контроллером и
колонкой. Третий пишет строки **SQL-запросом**, а не через репозиторий:
через репозиторий такую строку уже не создать, `Email` опустит её по
дороге, и по той же причине сама команда читает базу SQL-запросом.

Проверено тремя порчами: снятая нормализация, нормализация только домена
(вариант Django) и добавленный `strip()`. Первые две роняют тесты
локальной части и весь путь по HTTP; третья роняет проверки пробельных
символов — и заодно тест согласия схемы с доменом, который держит то же
правило со стороны веб-слоя.
