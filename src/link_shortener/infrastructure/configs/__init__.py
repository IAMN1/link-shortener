"""
Values a deployment settles, and the machinery that reads them.

What comes here is decided outside the code and read by it: the profile a
process runs under, the roles an installation starts with, the block the
worker is configured from. A constant the code chooses for itself is not a
setting and belongs beside the code that chooses it -- the question is
whether a deployment can be wrong about it.
"""
